<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [requestId=%X{requestId}] [clientIp=%X{clientIp}] [%thread] %c{1.} - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <RollingFile
                name="SecurityAudit"
                fileName="logs/security-audit.log"
                filePattern="logs/security-audit-%d{yyyy-MM-dd}-%i.log.gz">

            <PatternLayout pattern="${LOG_PATTERN}"/>

            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>

            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Logger name="SECURITY_AUDIT" level="info" additivity="false">
            <AppenderRef ref="SecurityAudit"/>
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="onon1101.lendingsystem" level="debug"/>

        <Logger name="org.springframework" level="info"/>

        <Root level="info">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
